# Node / frontend
node_modules/
dist/
build/
*.local
.vite/

# .NET / backend
bin/
obj/
# Bí mật cục bộ (khóa JWT, chuỗi kết nối DB, khóa mã hóa) — KHÔNG commit
appsettings.Local.json
!appsettings.Local.example.json
# Frontend build output gop vao backend (sinh ra boi `npm run build`)
backend/KetoanMini.Api/wwwroot/

# Desktop app / release artifacts
desktop/
release/
release-packager/
*.exe
*.msi
*.zip
*.asar
builder-debug.yml

# Local editor / agent config
.claude/
.codex/
.agents/
.codex-work/
.codex-spreadsheet-fix/
.codex-spreadsheet-inspect/

# Logs & moi truong
*.log
.env
.env.*
!.env.example
.codex-logs/
.codex-build/

# Local Codex/QA working data. Final source assets stay outside these folders.
tmp/
outputs/
artifacts/

# Chung chi HTTPS LAN - chua khoa bi mat, KHONG commit
backend/KetoanMini.Api/certs/
*.pfx
*.p12
rootCA-key.pem

# Rác do agent/skill sinh ra trong lúc phân rã BE — KHÔNG commit
.hallmark/
.claude/worktrees/

# Kết quả chạy test .NET
TestResults/
*.trx

# File rác do lỗi redirect "echo ... -> public" trong restart.bat (đã sửa).
# Giữ dòng này để lỡ chạy bản .bat cũ thì cũng không commit lại.
/backend/KetoanMini.Api/public
