# Node / frontend
node_modules/
dist/
build/
*.local
.vite/

# .NET / backend
bin/
obj/
# Bí mật cục bộ (khóa JWT, chuỗi kết nối DB, khóa mã hóa) — KHÔNG commit
appsettings.Local.json
!appsettings.Local.example.json
# Frontend build output gop vao backend (sinh ra boi `npm run build`)
backend/KetoanMini.Api/wwwroot/

# Desktop app / release artifacts
desktop/
release/
release-packager/
*.exe
*.msi
*.zip
*.asar
builder-debug.yml

# Local editor / agent config
.claude/
.codex/

# Logs & moi truong
*.log
.env
.env.*
!.env.example
.codex-logs/
.codex-build/
tools/camera-snapshot/runtime/
tools/camera-snapshot/logs/
tools/camera-snapshot/latest.jpg
tools/camera-snapshot/probe-*.jpg
tools/video-bridge/runtime/

# Chung chi HTTPS LAN - chua khoa bi mat, KHONG commit
backend/KetoanMini.Api/certs/
*.pfx
*.p12
rootCA-key.pem
