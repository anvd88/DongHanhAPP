# Node / frontend
node_modules/
dist/
*.local
.vite/

# .NET / backend
bin/
obj/
# Frontend build output gộp vào backend (sinh ra bởi `npm run build`)
backend/KetoanMini.Api/wwwroot/

# Logs & môi trường
*.log
.env
.env.*
!.env.example

# Chứng chỉ HTTPS LAN — chứa khóa bí mật, KHÔNG commit
backend/KetoanMini.Api/certs/
*.pfx
*.p12
rootCA-key.pem
