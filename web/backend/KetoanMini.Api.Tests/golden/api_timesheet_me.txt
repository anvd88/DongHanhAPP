# /api/timesheet/me
# Khung phản hồi theo vai trò. Sinh bởi GoldenContractTests; đừng sửa tay.

[Admin] 200 application/json
  $ : object
  $.days : array
  $.days[] : object
  $.days[].checkoutGraceMinutes : number
  $.days[].date : string
  $.days[].earlyMinutes : number
  $.days[].eventType : string
  $.days[].holidayName : string
  $.days[].holidayType : string
  $.days[].isOvernight : bool
  $.days[].lateMinutes : number
  $.days[].overtimeMinutes : number
  $.days[].shiftEnd : string
  $.days[].shiftName : string
  $.days[].shiftStart : string
  $.days[].status : string
  $.days[].workedHours : number
  $.period : string
  $.summary : object
  $.summary.absentDays : number
  $.summary.earlyDays : number
  $.summary.lateDays : number
  $.summary.totalEarlyMinutes : number
  $.summary.totalLateMinutes : number
  $.summary.totalOvertimeMinutes : number
  $.summary.totalWorkedHours : number
  $.summary.workedDays : number

[Accounting] 200 application/json
  $ : object
  $.days : array
  $.days[] : object
  $.days[].checkoutGraceMinutes : number
  $.days[].date : string
  $.days[].earlyMinutes : number
  $.days[].eventType : string
  $.days[].holidayName : string
  $.days[].holidayType : string
  $.days[].isOvernight : bool
  $.days[].lateMinutes : number
  $.days[].overtimeMinutes : number
  $.days[].shiftEnd : string
  $.days[].shiftName : string
  $.days[].shiftStart : string
  $.days[].status : string
  $.days[].workedHours : number
  $.period : string
  $.summary : object
  $.summary.absentDays : number
  $.summary.earlyDays : number
  $.summary.lateDays : number
  $.summary.totalEarlyMinutes : number
  $.summary.totalLateMinutes : number
  $.summary.totalOvertimeMinutes : number
  $.summary.totalWorkedHours : number
  $.summary.workedDays : number

[HR] 200 application/json
  $ : object
  $.days : array
  $.days[] : object
  $.days[].checkoutGraceMinutes : number
  $.days[].date : string
  $.days[].earlyMinutes : number
  $.days[].eventType : string
  $.days[].holidayName : string
  $.days[].holidayType : string
  $.days[].isOvernight : bool
  $.days[].lateMinutes : number
  $.days[].overtimeMinutes : number
  $.days[].shiftEnd : string
  $.days[].shiftName : string
  $.days[].shiftStart : string
  $.days[].status : string
  $.days[].workedHours : number
  $.period : string
  $.summary : object
  $.summary.absentDays : number
  $.summary.earlyDays : number
  $.summary.lateDays : number
  $.summary.totalEarlyMinutes : number
  $.summary.totalLateMinutes : number
  $.summary.totalOvertimeMinutes : number
  $.summary.totalWorkedHours : number
  $.summary.workedDays : number

[Driver] 200 application/json
  $ : object
  $.days : array
  $.days[] : object
  $.days[].checkoutGraceMinutes : number
  $.days[].date : string
  $.days[].earlyMinutes : number
  $.days[].eventType : string
  $.days[].holidayName : string
  $.days[].holidayType : string
  $.days[].isOvernight : bool
  $.days[].lateMinutes : number
  $.days[].overtimeMinutes : number
  $.days[].shiftEnd : string
  $.days[].shiftName : string
  $.days[].shiftStart : string
  $.days[].status : string
  $.days[].workedHours : number
  $.period : string
  $.summary : object
  $.summary.absentDays : number
  $.summary.earlyDays : number
  $.summary.lateDays : number
  $.summary.totalEarlyMinutes : number
  $.summary.totalLateMinutes : number
  $.summary.totalOvertimeMinutes : number
  $.summary.totalWorkedHours : number
  $.summary.workedDays : number

[Employee] 200 application/json
  $ : object
  $.days : array
  $.days[] : object
  $.days[].checkoutGraceMinutes : number
  $.days[].date : string
  $.days[].earlyMinutes : number
  $.days[].eventType : string
  $.days[].holidayName : string
  $.days[].holidayType : string
  $.days[].isOvernight : bool
  $.days[].lateMinutes : number
  $.days[].overtimeMinutes : number
  $.days[].shiftEnd : string
  $.days[].shiftName : string
  $.days[].shiftStart : string
  $.days[].status : string
  $.days[].workedHours : number
  $.period : string
  $.summary : object
  $.summary.absentDays : number
  $.summary.earlyDays : number
  $.summary.lateDays : number
  $.summary.totalEarlyMinutes : number
  $.summary.totalLateMinutes : number
  $.summary.totalOvertimeMinutes : number
  $.summary.totalWorkedHours : number
  $.summary.workedDays : number
