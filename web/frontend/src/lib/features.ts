/**
 * Cờ bật/tắt tính năng ở frontend.
 *
 * IP_CAMERA_ENABLED: tính năng chấm công + xem hình bằng CAMERA IP (RTSP kiosk) và thẻ
 * "Chấm công tự động" ở trang Hệ thống. Đang TẠM TẮT theo yêu cầu. Bật lại: đổi về true
 * (và đặt KioskCamera:Enabled = true ở backend appsettings rồi khởi động lại API).
 */
export const IP_CAMERA_ENABLED = false;
